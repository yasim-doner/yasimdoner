services:
  portfolio:
    build:
      context: .
      dockerfile: Dockerfile
    image: yasimdoner-portfolio:latest
    container_name: yasimdoner-portfolio
    ports:
      - "${PORT:-3000}:80"
    restart: unless-stopped
